# START vulnerability: database authentication is intentionally reachable from any lab zone.
local   all             all                                     trust
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::/0                    scram-sha-256
